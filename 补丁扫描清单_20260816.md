# AuroraDrive 补丁（workaround）扫描清单（2026-08-16）

3 个工程师按模块只读扫描，共 24 个疑似补丁。**分三类**：真补丁（可重构）/ 需外部条件 / 误报（正规设计，不能动）。

## A. 真补丁（该重构，能正规化）— 11 个

| # | 位置 | 补丁 | 躲开了什么 | 正规做法 |
|---|---|---|---|---|
| 1 | AuroraDriveApp:869 | `effectiveSpeed *= 0.9` 假速度衰减 | OCR 读不到速度 | 真速度估计器，或明确"无速度"状态而非伪造衰减 |
| 2 | AuroraDriveApp:813 | `effectiveSpeed -= 6` 待机魔法衰减 | 同上 | 同上 |
| 3 | AuroraDriveApp:842 | `confidence: 0.9` 占位 | 模型无结果 | 统一置信度模型 |
| 4 | EscapeController:110 | `Bool.random()` 随机脱困方向 | 哪侧可通行无感知 | 用 YOLO 检测框判断可通行侧（YOLO 已有检测能力） |
| 5 | EscapeController:24,143 | 脱困固定 `confidence 0.3` | 无真实来源 | 同上 |
| 6 | RuleController:121-148 | 硬编码置信度 0.4/0.5/0.6/0.7 | 无真实度量 | 用检测框质量/TTC 算真实置信 |
| 7 | ConfidenceEstimator:155,183 | 帧数不足返 0.8、无图返 0.5 | 无数据 | 统一置信度模型 |
| 8 | ~~SpeedOCRReader:555-563 ±1 像素 9 偏移投票~~ | ~~裁剪窗与字模 1px 错位~~ | **已重新定性为正规设计（见 C 类 #19），非补丁** |
| 9 | SpeedOCRReader:63,67,73 | 槽位魔法值 0.022→0.014 反复手改 | 槽位几何手工实测 | 标定一次写死 + 回归断言 |
| 10 | SpeedOCRReader:312-315 | 速度跳变>60 沿用旧值 | 读错 | 跳变标记低置信而非静默沿用 |
| 11 | InferenceEngine:268-274 | readScalar 缺特征 return 0 | 缺特征 | 报错/标记，不默认为 0 |

## B. 需外部条件（真补丁但暂时无法完全正规）— 1 个

| # | 位置 | 补丁 | 说明 |
|---|---|---|---|
| 12 | InferenceEngine:320-345 | 无遥测时喂假 vehicle_state（curvature=0, heading=0/1, speed/120） | 正规需游戏遥测数据源，目前没有。可保留但加注释标明"训练分布内占位" |

## C. 误报（看似补丁，实为正规设计，不能动）— 6 个

| # | 位置 | 为何不能动 |
|---|---|---|
| 13 | ControlEngine:162-167 `refreshHeldKeys` 重发 keyDown | **这是之前修"M9 出不了键"的关键修复**：CGEvent 是一次性事件，不像真键盘持续上报，必须周期重发 keyDown（auto-repeat），否则事件流干涸、游戏收不到键。删了 M9 直接开不了车。 |
| 14 | ControlEngine:172-182 `releaseAll` 无条件 keyUp | 释放系统残留卡键的正规做法，删了会残留卡键。 |
| 15 | DegradeStateMachine:40 脱困 30s 超时兜底 | 正规的降级出口（刚修 bug 3 加的），不是补丁。 |
| 16 | CaptureEngine:183-188 config.width×backingScaleFactor | 点值→像素的正确换算（Retina 2x），是正规做法，非绕 API。 |
| 17 | speedROINorm/trackLock 魔法值 | 标定/调参值，是正常常数，不是躲开问题的补丁（但可加注释说明来源）。 |
| 18 | RecordEngine 背压阈值 3 vs 1 不一致 | 这是不一致 bug（不是补丁），统一抽常量即可，属重构小项。**已修（抽统一常量 maxPendingWrites=1）** |
| 19 | SpeedOCRReader:540-593 ±1 像素 9 偏移投票 | **经逐像素验证（mean_abs_diff=0.0），cropSlots 与训练端零误差；投票是为速度表数字"帧间亚像素抖动"设计的鲁棒机制**（slot0 零偏移 81/81，slot1/2 ±1~2px 无固定方向）。去掉必丢帧（000095/000096 错读 018→010、000080 丢帧）。是正规鲁棒设计，保留。 |

## 重构结果（截至 2026-08-16 12:5x）
- ✅ **已消除真补丁 2 个**：#10 速度跳变静默沿用旧值（改标记低置信走正规降级）、#18 背压阈值不一致（抽统一常量）。
- ⚠️ **#8 ±1 投票重新定性为正规设计**（帧间亚像素抖动的鲁棒机制，非补丁），保留。
- ⏸️ 剩余真补丁：#1/#2 假速度衰减、#4 随机脱困方向、#6/#3/#5/#7 置信度族——需接入真实数据源（遥测/YOLO 可通行/检测框质量）或涉及驾驶安全，待用户决策。

## 建议重构顺序
A 类 11 个真补丁 → 按风险从低到高：先低风险（#9 槽位回归断言、#11 readScalar、#18 阈值统一、#10 跳变标记）→ 再中风险（#1/#2 速度估计、#7/#3/#6 置信度统一）→ 最后高风险（#4 随机脱困方向，涉及驾驶安全，需最谨慎）。

**每批走流程**：架构师设计 → 工程师重构 → QA → 交叉审查，且先备份。
