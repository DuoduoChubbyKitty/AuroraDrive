
## 地图图层开关接活 + 换底图（2026-08-18 晚，用户选方案A）
- 用户澄清：旧底图 `enhanced_1366.png` 是他自己当年做的缓兵之计（一张无内容超分大图），**已废必须删**（用户手动删文件）；图层/标记数据老同理要换。
- **换底图**：从 MaaNTE 拷官方大世界地图 `bigworldmapSecond.png`（11264×11264，6.9MB，本地盘免费）到 `models/bigworldmapSecond.png`；`GameMapView.swift` 的 `mapImagePath` 改指向它。`altMapImagePath` 仍 `yihuan_map_z4.png`。
- **图层开关死穴定位**：`filteredMarkers` 原被写死 `return []`（用户当年短路的），导致侧栏分类开关/全选/清空点了没反应。已接活：读 `categories.isEnabled` + 搜索过滤，图层浮层/筛选侧栏共享同一份状态。
- **新增常驻图层浮层**：顶部 📑 按钮（`layerToggleButton`）从右侧弹簧拉下 `layerPanel`（300px 玻璃面板，列出 15 类图层，可单独开关 + 全选/清空），不依赖模式，契合用户"拉下来"需求。
- **默认图层状态**：核心导航层（waypoint/region/phone-booth/tower）默认开；收集类（材料/宝箱/谕石/收集品/怪物/BOSS…）默认关，全收集时在浮层手动开。
- 初始缩放 0.15→0.11（适配 11264 宽图看全貌）。
- **编译部署**：`swift build -c release` → BUILD_EXIT=0；cp 到 AuroraDriveUI + AuroraDrive.app/Contents/MacOS/AuroraDrive，两个目标 codesign adhoc 均 valid。⚠️重签后需用户重勾屏幕录制/辅助功能 TCC 权限并完全退出重启才生效。
- **已知限制**：标记坐标仍是旧库百分比体系，换 11264 底图后**可能漂移**（未配准）。待用户本机重抓 nteguide 最新数据覆盖 `FINAL_complete_map_database.json` 后，需用 MaaNTE `coordinate_position.py` 的 `CALIBRATION_A/B/TX/TY` 仿射把百分比坐标配准到新底图。
