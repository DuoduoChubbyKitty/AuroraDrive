# AuroraDrive 主线程延迟深度审计报告（2026-08-15）

## 审计方法
5 名工程师子 agent 并行全量读取全部 14 个源文件（共 ~7500 行），只读不改。
审计触发数据：主线程 tick 实际仅 **~7.7Hz**（日志 `tickGap=103~152ms`，本应 30Hz ≈ 33ms）；
延迟**随运行时间累积恶化**（优化后从 1–2 分钟推迟到 4–5 分钟才明显，但仍存在）；
`capGap=33ms`（捕获没掉帧）、`capWork` 正常（每帧处理不重）→ 瓶颈在**主线程 tick 单次耗时 ~100ms**。

## 一致共识：真凶是三处叠加
1. **CoreML ANE 持续过载（根因级）**：3 个模型（M9 ≈24Hz + assist ≈24Hz + YOLO ≈30Hz ≈ **78 次/秒**）全部 `computeUnits=.all` 抢同一块 ANE → 持续热节流 → 后台推理完成回调的 `Task{@MainActor}` hop 在主线程堆积 → **越跑越卡**（1–2min→4–5min 正是节流爬坡期）。
2. **每 tick 主线程重活**：≤3 次全屏 `cgImage` 光栅化 + SwiftUI 整树重绘 + 按键注入 CGEvent 内核 I/O。
3. **captureQueue 隐性成本**：每帧 22MB 原生帧 memcpy、YOLO 跨主线程 actor hop、YOLO 源头 640 缩放。

## 延迟热点清单（文件:行号 / 做什么 / 量级 / 削减方案 / 触碰30fps红线）

### A. CoreML 推理（最关键）
| 位置 | 做什么 | 量级 | 削减方案 | 红线 |
|---|---|---|---|---|
| InferenceEngine:123 / YoloEngine:143 | 三模型全 `.all` 抢 ANE，78次/秒 | 随时间↑↑（热节流） | **分流 computeUnits**：M9/assist 固定 `.cpuAndGPU` 或 ANE，YOLO 改 `.cpuOnly` | **不碰频率，安全** |
| InferenceEngine:161 ×2 | 每 tick 主线程 `cgImage(forProposedRect:)` 光栅化全屏图 | 20~60ms | infer 改收 `CVPixelBuffer`（与 OCR 同源复用），消除重复转换 | 不碰，安全 |
| ConfidenceEstimator:177 | 第 3 次主线程 `cgImage`+32² draw | ~1ms | 合并一次提取 | 不碰，安全 |
| InferenceEngine:156 | infer 无时间闸，满频触发 H1 | 放大 H1 | 见下"需你拍板" | 见下 |

### B. SwiftUI 重绘
| 位置 | 做什么 | 量级 | 削减方案 | 红线 |
|---|---|---|---|---|
| AuroraDriveApp:391 | `currentScreenImage` 未标 `@ObservationIgnored`，每 tick 换 NSImage → GameViewportView(:1071)+ObstacleOverlay(:1113) 整树 30Hz 重建 | 中 | 改独立子视图直绘 CGImage；`detections` 等加 `@ObservationIgnored` 隔离 | 不碰，安全 |
| YoloEngine:312 | `finish` 主线程写 detections/lockedTarget | 每帧触发 Overlay 重绘 | 平滑/锁定移 inferenceQueue，主线程只收结果 | 不碰，安全 |

### C. 捕获与帧管线
| 位置 | 做什么 | 量级 | 削减方案 | 红线 |
|---|---|---|---|---|
| CaptureEngine:226 / 311–338 | `copyNativeFrame` 每帧同步 memcpy 22MB 整帧（OCR 只需 5Hz） | 5~15ms/帧 | 仅 OCR 周期触发时才拷（本质是节流→**你之前否决过节流，需重议**） | 触碰"不节流"红线 |
| CaptureEngine:232–256 → YoloEngine:36 | `onYoloFrame`/`inferFast` 是 `@MainActor`，captureQueue 跨 actor hop 回主线程做 0.5MB 拷贝 | 阻塞 captureQueue | 回调内 `DispatchQueue.async` 派发 inferenceQueue | 不碰，安全 |
| CaptureEngine:245–252 | YOLO 源头 GPU 缩放 **640**（非注释说的 352） | 计算量≈352 的 3.3× | 评估模型能否用 352 输入 | 不碰，安全 |
| YoloEngine:215 / 303–320 | `finish()` 主线程 `smooth`（IoU N²）+`trackLock` | 持续吃主线 | 移 inferenceQueue | 不碰，安全 |

### D. 控制与决策链
| 位置 | 做什么 | 量级 | 削减方案 | 红线 |
|---|---|---|---|---|
| ControlEngine:162–167 `refreshHeldKeys` | 每帧对每个按住键发 CGEvent 内核 HID 投递（保游戏"键仍按着"） | **~100ms 预算大头** | 边沿重发+降频 tickle（须保游戏判键按着） | **必要 I/O，触碰红线，改前必问** |
| ControlEngine:194–217 `postKeyEvent` | 每次创建 CGEvent+post+计数 | 30Hz×键数累积 | 同上 | 同上 |
| ConfidenceEstimator:175–208 | 每帧图像解码+draw 算亮度 | 中 | 亮度预计算移后台 | 不碰，安全 |

### E. 录制与地图（非主因）
- RecordEngine：主线程仅微秒级，编码/写盘全在后台 `writeQueue`，且**仅录制时触发**，不录制零影响。
- GameMapView：独立 `.sheet`，不进 tick；仅打开瞬间一次性加载巨图(~100–300ms)，与 30Hz tick 解耦。**非 7.7Hz 主因**。

## 安全可立即做（不降频、不碰红线）
1. **分流 ANE**（InferenceEngine:123 / YoloEngine:143）：改 `computeUnits` 分布 → 直击"越跑越卡"根因。
2. **消除每 tick 3 次主线程 cgImage 提取**（InferenceEngine:161 / ConfidenceEstimator:177 / SpeedOCRReader cropSlots）：改收/复用 CVPixelBuffer。
3. **SwiftUI 重绘隔离**（AuroraDriveApp:391 / YoloEngine:312）：`@ObservationIgnored` + 独立子视图。
4. **YOLO 跨主线程 hop 改异步派发**（CaptureEngine:232–256）+ **finish 平滑移后台**（YoloEngine:215/303–320）。
5. **YOLO 源头缩放 640→352 评估**（CaptureEngine:245）。

## 需你拍板（触碰"不降频/不节流"红线）
- **A-4 / C-1**：infer 加时间闸、copyNativeFrame 节流到 5Hz —— 你之前明确否决"任何节流"，这两项与铁律冲突，**不动**。
- **D 按键注入**：refreshHeldKeys 是 ~100ms 主线程 I/O 最大户，但它是"游戏收到持续按键"的必要机制，且你定 30Hz 注入红线，**不能降频/改边沿前必须你点头**。

## 下一步
按"先问后改"铁律，待你确认后，优先落地 **安全 5 项**（尤其 A-1 分流 ANE + B 重绘隔离），可显著压低主线程 tick 耗时、消除"越跑越卡"。D 项与节流项需你单独拍板。

---

## 第二轮精确归因（2026-08-15 20:3x，5 工程师逐行追，修正第一轮）
**修正第一轮误判**：
- refreshHeldKeys 按键注入 **不是**瓶颈（Agent 控制审计确认每帧 <0.5ms，CGEvent.post μs 级，3-5 键×30Hz 远不够 100ms）——第一轮把它列为"~100ms 大头"是**错的**。
- copyNativeFrame 22MB memcpy 在 captureQueue 后台、OCR 有 5Hz 节流，对 tick 直接贡献**低**（但 autorelease 累积是"随时间恶化"的次因）。
- CIContext 复用正确（不每帧新建）、nativePool 真复用、YOLO 实际 640（非注释 352）——均排除为热点。

**tick ~100ms 确定性归因（主线程同步重活，非调度问题）**：
tick() 同步调用 **3 次全屏 `cgImage` 光栅化**（这是大头，占 ~60-90ms）：
1. InferenceEngine:161 `cgImage(forProposedRect:)` 在 `inferenceQueue.async` **之前**于主线程执行（M9 infer 一处 + assist infer 一处 = 2 次全屏解码，因 `infer` 是 `@MainActor`，光栅化在派发后台队列前就做了）
2. ConfidenceEstimator:177 第三次全屏 cgImage（亮度采样前先全屏栅格化，imageWeight 仅 0.2 却付全价）
3. + SwiftUI 整树重绘：`currentScreenImage`(:391) 未标 `@ObservationIgnored`，每 tick 赋新 NSImage → GameViewportView(:1071) 全屏 `Image(nsImage:)` 重建，与 tick 抢主线程

**"随运行时间恶化"Top 2 确定性根因**：
1. **ANE thermal 节流（主因）**：M9/assist/YOLO 全 `.all` 抢 ANE（≈78次/秒）持续压 → 系统级 thermal 升 → 整 SoC（含跑 tick 的 CPU）统一降频 → tickGap 单调 103→152ms。最经典机制，不依赖帧尺寸。
2. **内存压力→压缩/换页（次因）**：全工程 **0 处 autoreleasepool**；每帧 createCGImage+NSImage 累积；RecordEngine:246 闭包强捕获 NSImage 堆积 → RSS 涨 → 内存压缩/换页 → 每帧变慢。这正是"22MB→480px 把 1-2min 推迟到 4-5min"的根由（分配降 1/37 缓解内存压力）。

**缺失**：三模型无 warm-up（首帧延迟高，但非"随运行恶化"主因）。

**精确削减方案（全安全，不降 30fps 红线）**：
- A1. `infer` 入口改收 CGImage/CIImage 或 CVPixelBuffer，把 InferenceEngine:161 全屏光栅化移入 inferenceQueue 后台（M9+assist 各一次）
- A2. ConfidenceEstimator.computeImageScore 改后台/低频/降采样，去除全屏栅格化
- A3. SpeedOCRReader.cropSlots 主线程 createCGImage×3 移入 ocrQueue
- B. `currentScreenImage` 标 `@ObservationIgnored` + 独立子视图直绘，隔离 SwiftUI 重绘
- C. computeUnits 分流：M9/assist 固定 `.cpuAndGPU`(或 `.gpuOnly`)，YOLO 改 `.cpuAndGPU`（640 太大 cpuOnly 不可行）；补 warm-up
- D. autoreleasepool：captureQueue 的 `createCGImage` 裹池；RecordEngine appendFrame 弱捕获 + 池内转 Data 即丢引用
- **结论**：核心 60-90ms 主线程重活（3 次全屏栅格化 + 重绘）靠 A/B 砍掉；越跑越卡靠 C/D 治。均不降频、不碰红线。
