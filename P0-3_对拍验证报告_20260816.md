# P0-3 对拍验证报告 — AuroraDrive YOLO 输出格式与通道序

**日期**：2026-08-16
**验证人**：CoreML/YOLO 验证专家（自动化）
**结论**：**P0-3 关闭 ✅。通道序正确、坐标正确、无"避障静默失效"风险。**

---

## 1. 验证方法与样本

| 项 | 值 |
|---|---|
| 样本图 | `/Users/dupi/Desktop/游戏画面_速度测试.png`（2940×1912 游戏帧，多车清晰可检） |
| 模型 | `models/yolo26s.mlmodelc`（Swift 用）/ `yolo26s.mlpackage`（Python 用） |
| Swift 自检 | `.build/release/AuroraDrive --yolo-selftest /Users/dupi/Desktop/游戏画面_速度测试.png` |
| Python 基准 | `tools/verify_yolo26_pair.py`（新写）+ inline 决定性对照 |
| 置信度阈值 | 0.22（与 Swift 自检一致） |

---

## 2. 模型 description 元数据（关键证据 ①）

`coremltools.models.MLModel('models/yolo26s.mlpackage').get_spec().description`：

```
INPUT  name=image   640×640   colorSpace=20  (= RGB)        ← 模型声明要 RGB
OUTPUT name=var_1442  shape=[1, 300, 6]  dtype=FLOAT32
```

`FeatureTypes_pb2.ImageFeatureType.ColorSpace.Name(20) = "RGB"`（protobuf 枚举核实）。

**结论**：模型期望输入是 RGB 顺序的图像。

训练端推导：Ultralytics YOLO 训练时通过 `v8_transforms.Format` 类将 cv2 读入的 BGR 图像通过 `[::-1]` 翻转为 RGB 后送入网络（ultralytics 标准行为）；导出 CoreML 时 `model.export(format="coreml")` 自动把 imageType 标记为 RGB，与训练一致。

---

## 3. Swift 自检实际输出（BGRA 缓冲，ANE 推理）

```
图片: /Users/dupi/Desktop/游戏画面_速度测试.png  (1470×956  NSImage 点尺寸；像素 2940×1912)
模型: yolo26s.mlmodelc
耗时: 8.6 ms  阈值: 0.22
输出dtype: 65568  shape=[1, 300, 6]    ← 65568 = FLOAT32
原始输出行[0..12] (x1,y1,x2,y2,conf,class_id):
   0 (206.000,341.750,401.750,612.000) conf=0.7148 cls=2   ← 前车（玩家车）
   1 (149.125,252.125,204.000,313.000) conf=0.5352 cls=7   ← 对向 SUV
   2 (245.375,218.625,271.500,235.375) conf=0.4961 cls=2
   3 (149.125,252.125,204.000,313.000) conf=0.3477 cls=2
   4 (172.625,236.875,195.125,252.375) conf=0.3389 cls=2
   5 (118.625,221.500,145.375,245.000) conf=0.3018 cls=7
   …
过阈值 7 个:
  CAR 0.715  中心(0.475,0.745) 宽高(0.306,0.422) [危险区]
  TRUCK 0.535  中心(0.276,0.442) 宽高(0.086,0.095)
  …
```

> 注释：Swift 输出 dtype=65568 经查 `FeatureTypes_pb2.ArrayFeatureType.ArrayDataType.FLOAT32 = 65568`，所以 Swift 拿到的就是 Float32，与 spec 一致。

---

## 4. 坐标格式判断（关键证据 ②）

- Row 0 原始值：`x1=206, y1=341.75, x2=401.75, y2=612`
- 范围均在 0~640，与 640×640 输入边长相符
- 若为归一化坐标，值应 ≤ 1；这里显然 > 1 → **像素坐标**
- Swift `parse()` 用 `x1 = readML(...)/size`（size=640）转归一化，与 Detection 契约一致
- Python 端同样按"像素 / 640"解读 Swift 输出，得到 `(cx=0.475, cy=0.745, w=0.306, h=0.422)`，与 Swift 自检"CAR 0.715 中心(0.475,0.745) 宽高(0.306,0.422)"**逐字段一致**

→ **坐标格式 = 像素(0~640)，Swift parse() /640 正确 ✅**

---

## 5. 通道序对拍（关键证据 ③，决定性）

### 5.1 双向比对

| 路径 | 输入 | top-1 (cls=2 玩家车) 框 | top-1 conf |
|---|---|---|---|
| **A** | PIL `Image.open(...).convert("RGB").resize(640)`（训练端标准）| (205.25, 341.5, 402.0, 610.0) | 0.9170 |
| **B** | 同上但通道倒置 `[..., ::-1]`（喂 BGR 字节） | (205.375, 341.5, 401.25, 609.0) | 0.9365 |
| **C** | `Image.open('/tmp/yolo_swift_input.png').convert("RGB")`（Swift 实际喂的字节）| (206.0, 341.75, 401.75, 612.0) | **0.7148** |
| **Swift** | `makePixelBuffer(32BGRA) + draw()` → CoreML 直接推理（ANE）| (206.0, 341.75, 401.75, 612.0) | **0.7148** |

**Swift ANE 结果 与 Python CPU 跑同一字节（C 行）conf 完全相同 0.7148，box 完全一致。**
**Swift ANE 结果 与 Python CPU 喂 RGB 的 PIL resize 输入（A 行）box 一致到 1px 内，conf 差异（0.7148 vs 0.9170）100% 来自不同 resize 实现（CGContext .low vs PIL BILINEAR），与通道序无关。**

### 5.2 12 行 raw 输出逐行比对（同 cls + 中心距离最小配对）

| k | Swift 框 (x1,y1,x2,y2) | Swift conf | Python [A] 框 | Py[A] conf | Δdist |
|---|---|---|---|---|---|
| 0 | 206.0, 341.75, 401.75, 612.0 | 0.7148 | 205.25, 341.5, 402.0, 610.0 | 0.9170 | 1.15 px |
| 1 | 149.125, 252.125, 204.0, 313.0 | 0.5352 | 149.125, 252.0, 203.875, 313.5 | 0.4736 | 0.20 px |
| 2 | 245.375, 218.625, 271.5, 235.375 | 0.4961 | 245.125, 218.625, 273.75, 236.375 | 0.5273 | 0.81 px |
| 3 | 149.125, 252.125, 204.0, 313.0 | 0.3477 | 149.125, 252.0, 203.875, 313.5 | 0.3984 | 0.20 px |
| 4 | 172.625, 236.875, 195.125, 252.375 | 0.3389 | 172.125, 236.5, 195.625, 252.25 | 0.3955 | 0.25 px |
| 5-12 | （后 7 行同样 cls 一致，Δdist ≤ 1.15 px） | | | | |

**12/12 同 cls 中心距离 ≤ 1.15 px 全部匹配 ✅**

### 5.3 直接像素证据

Swift 的 `dumpPNG` 把 32BGRA 缓冲导出 PNG。把这张 PNG 当 RGB 读回来，与 PIL resize RGB 比：
- 与 PIL RGB（正确通道序） 像素差：max=141, mean=6.290（CGContext vs PIL 插值器差异）
- 与 PIL RGB 通道倒置（[..., ::-1]）像素差：max=239, mean=**24.138**

如果 Swift 真的把通道搞反了（把 B 写到 R 位置），dumpPNG 出来的 PNG 会显示出通道错位的图，mean 差会接近 24 而非 6.3。

→ **Swift 喂入模型的 32BGRA 缓冲的 R/G/B 三通道在物理字节序上就是 RGB，没有颠倒 ✅**

### 5.4 CoreML 转换机制

Swift 喂入 `kCVPixelFormatType_32BGRA` 像素缓冲（内存字节序 B,G,R,A）→ CoreML 知道 buffer 格式为 32BGRA → 模型 `imageType.colorSpace = RGB` 要求 RGB 输入 → CoreML 运行时自动从 32BGRA buffer 抽取 R/G/B（byte[2]/byte[1]/byte[0]），丢弃 A（byte[3]），按模型期望的 RGB 顺序送入网络 → **通道转换由 CoreML 自动完成，App 不需要做额外处理。**

注释 YoloEngine.swift:672-673 写的「32BGRA + premultipliedFirst + littleEndian32 == BGRA 内存序，正好对上模型声明的 colorSpace: BGR」是**与 FastestV2 旧模型（colorSpace=BGR）配套的过期注释**，但当前模型声明是 RGB，机制仍然正确——只是注释描述反了（应当是"CoreML 看到 32BGRA buffer，按模型声明的 RGB 抽取通道"）。

---

## 6. 副带结论

1. **坐标格式确认 = 像素**：output `[1,300,6]` 中 x1,y1,x2,y2 范围 0~640，与 640×640 输入相符。Swift `parse()` 的 `/inputSize` 是必需的；如果改成"假设归一化直接用"，所有框会塌成 <1/640 的小点，避障完全失效。本次确认 `/640` 正确。
2. **置信度 Float32**：模型 spec 声明 `dataType: FLOAT32`，Swift `MLMultiArray` 拿到的 dtype=65568 = FLOAT32（已查 protobuf 枚举），与 spec 一致；不是 Float16。
3. **CocoLabels.names 与模型 metadata.names 完全一致**：COCO-80 顺序，class 2=car，0=person，9=traffic light 等。映射无误。
4. **同名 box 不同 cls/conf 的现象**：Swift row 1 与 row 3 像素相同但 cls=7/2、conf=0.5352/0.3477；row 5/6 同理。这是 YOLOv26 e2e 输出对同一个 anchor 同时给所有 80 类预测 + 类内 sigmoid 后的正常行为，Swift `parse()` 按 conf 排序截断后正确处理。
5. **conf 差异 0.7148 vs 0.9170 来自输入像素差异**（CGContext low-quality 插值 vs PIL BILINEAR），不是通道序问题，已通过"Python 跑 Swift 实际输入字节"精确匹配 0.7148 锁定根因。

---

## 7. P0-3 关闭判定

| 检查项 | 期望 | 实际 | 判定 |
|---|---|---|---|
| 模型 imageType.colorSpace | RGB | RGB (枚举 20) | ✅ |
| Swift 喂入缓冲通道序 | RGB 字节序（被 CoreML 正确识别） | 32BGRA buffer，物理字节 B,G,R,A — CoreML 自动抽取为 RGB | ✅ |
| 通道序是否颠倒 | 否 | 否（与 Python RGB 像素差 mean=6.3；与倒置 mean=24）| ✅ |
| 输出坐标格式 | 像素 0~640 | 像素 0~640 | ✅ |
| Swift parse() `/640` | 应除 | 已除 | ✅ |
| Swift 与 Python 同一图位置匹配 | 同 cls、<5px | 12/12 ≤ 1.15 px | ✅ |
| Swift 与 Python 同一字节 conf 匹配 | 一致 | 完全一致 0.7148 | ✅ |

**P0-3（YOLO 像素 vs 归一化、BGR vs RGB）已关闭，无静默失效风险。**

---

## 8. 备注（非 P0-3 范围，顺手记录）

- 三个部署二进制 MD5 当前**不一致**（`AuroraDriveUI`、`.build/release/AuroraDrive`、`AuroraDrive.app/Contents/MacOS/AuroraDrive` 各不同）：按项目铁律应执行 `cp .build/release/AuroraDrive AuroraDriveUI && cp .build/release/AuroraDrive AuroraDrive.app/Contents/MacOS/AuroraDrive && codesign --force --deep --sign - AuroraDrive.app`。本任务范围只读 + 跑命令，不做部署同步。
- YoloEngine.swift:11-12 注释「colorSpace = BGR」是 FastestV2 旧注释，未随 yolo26s 替换更新为 RGB。建议下次修改时一并更新（但不是 bug）。
- `tools/verify_yolo26_pair.py` 是本次新增的对拍工具（非业务代码），可作为后续回归脚本。