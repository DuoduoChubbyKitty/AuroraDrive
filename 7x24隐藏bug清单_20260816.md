# AuroraDrive 7×24 隐藏 bug 清单（2026-08-16 深度排查）

5 个方向并行排查：内存泄漏 / 并发锁 / 录制磁盘 / 数值时序 / 推理状态机。共 27 个，按严重度分级。

## P0 高危（崩溃 / 死锁 / 永久停摆）
1. **`Int(NaN)` trap 崩全车** — YoloEngine.swift:695 `Int(Self.readML(...).rounded())`，YOLO 输出 NaN/Inf 时 Int() 直接 crash。修复：先 isFinite 判定或 clamp。
2. **脱困无限循环** — AuroraDriveApp.swift:909 每帧 `if phase==.done { enter() }` 重新脱困，抵消 EscapeController 15s 超时；7×24 永不停歇"倒车→转向→前进"。修复：一次 enter 只许一次周期 + 最大次数。
3. **脱困退出依赖 OCR，OCR 失效即死锁** — DegradeStateMachine.swift:124 退出条件 `speedValid && speedKmh>6`，OCR 不新鲜时 speedValid 永 false → 永久卡 .recover。修复：加转向/油门历史或倒计时兜底出口。
4. **YOLO 错误路径不复位 isInferencing** — YoloEngine.swift:275 `guard let dst else { return }` 不调 finish，isInferencing 永久 true，YOLO 停摆。修复：guard 失败补 finish/defer 复位。
5. **诊断属性裸跨线程读写（torn read → NaN trap）** — CaptureEngine.swift 的 isCapturing/captureFPS/lastFrameGapMs 等写于 captureQueue、读于主线程，无锁；`Int(Double.nan)` trap 崩溃。修复：原子类型/锁。
6. **模型加载失败主线程重试风暴** — InferenceEngine:184/YoloEngine:190 加载失败后下一 tick 主线程同步 MLModel() 30Hz 无限重试，主线程卡死。修复：冷却时间/移后台。
7. **文件句柄泄漏 + 误关新句柄** — RecordEngine.swift:146/191 stop 异步 close + start 覆盖引用 → 旧句柄泄漏、异步块误关新句柄 → 崩溃/CSV 缺行。修复：主线程同步 close。
8. **录制数据无限累积** — 224×224 JPEG 30fps ≈ 50GB/天，无上限无自动清理（glyph_clips 永不清理），7×24 撑爆磁盘。修复：磁盘配额/自动淘汰最旧 clip。

## P1 中危（性能退化 / 资源累积）
9. **captureQueue 无 autoreleasepool** — CaptureEngine.swift:215 SCStream delegate 回调不自动包池，30fps 对象累积。修复：stream() 体包 autoreleasepool。
10. **OCR CIContext 缓存累积** — SpeedOCRReader.swift:454 `CIContext()` 未设 cacheIntermediates:false。修复：加 options。
11. **录制强捕获 NSImage + 池扩容** — RecordEngine.swift:242 [weak self, image] 强捕获，背压 3 + 显示 + 双推理链路叠加持缓冲，池自动扩容不回缩。修复：传缩放小图/背压降 1/块内 autoreleasepool。
12. **dlog 日志无限增长** — AuroraDriveApp.swift:305 每秒追加不截断，86400 行/天直到磁盘满。修复：文件大小上限/轮转。
13. **地图 4K 图常驻** — GameMapView.swift:1537 [self] 强捕获 + 8636×8592 ≈280MB 常驻。修复：weak self + 关闭置 nil。
14. **完成回调无 generation 防过期** — InferenceEngine/YoloEngine 无 generation 校验（仅 OCR 有），reset 后旧结果写回误判存活。修复：仿 OCR 加 generation。
15. **InferenceEngine 每帧新建 MLMultiArray** — 1×3×180×320 ≈691KB ×24Hz ≈16MB/s 分配抖动。修复：复用缓冲。
16. **亮度 in-flight 卡死** — ConfidenceEstimator.swift:187 brightnessInFlight 置 true 后后台异常不清除，之后永久用缓存分。修复：defer/失败路径清除。
17. **卡死检测被 speedValid 门控漏检** — DegradeStateMachine.swift:168 OCR 失效时 stuckSeconds 永不累积，车真卡死不进脱困。
18. **CSV 无 fsync** — RecordEngine.swift:261 崩溃/断电丢尾/半行。修复：close 前 synchronizeFile。
19. **stop 提前 return 漏关句柄** — RecordEngine.swift:162 guard 在 isRecording=false 后 return，句柄不关。修复：close 移到 guard 前。

## P2 低危
20. stop() 不清池（8MB 常驻）。
21. RecordEngine `%06d`/`%d` 传 Int64 变参类型不匹配（UB）。
22. AuroraDriveApp:805 硬编码 dt=1/30 导致计时漂移。
23. `--auto-seconds NaN` 会 trap（自测入口）。
24. lastFrameGapMs 首帧 distantPast 显示 ~5e11 ms。
25. DegradeStateMachine:171 阈值滑到 1.0 理论除零。
26. csvHandle 跨线程共享无显式同步（隐性约定）。
27. 中途切 glyphMode 被静默忽略。

## 修复建议顺序
P0（1-8）先修 → P1（9-19）→ P2（20-27）。每批修完编译（看真实 exit code）+ QA + 交叉审查。
