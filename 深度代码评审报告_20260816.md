# AuroraDrive 深度代码评审报告

> 评审日期：2026-08-16
> 评审方法：5 个并行子 Agent 分模块深挖（推理决策 / 捕获控制安全 / 感知 YOLO·OCR / App壳·UI·录制 / Python 训练链路）+ 主线程亲自交叉验证核心文件（DegradeStateMachine / EscapeController / AuroraDriveApp 关键段）。累计工具调用 >300 次。
> 评级标准（用户给定四档）：**较好 < 良好 < 极好 < 极致**

---

## 一、整体评级：🟡 良好（Good）

**一句话结论**：这是工程完成度很高、底层推理契约经实测可信、30fps 红线架构精良的项目；但存在 1 处铁律正面违反、1 处可被 UI 触发的永久死锁、若干"绕行式补丁"残留、感知层关键正确性假设未对拍验证、以及训练-推理 state 错位未根治——这些系统性缺陷把它挡在"极好/极致"之外。

**为什么不是"较好"**：底层远不止"基本能跑"。R/B 通道未交换、画面无垂直翻转、`MLMultiArray[[0,0]]` 读取正确、训练侧/导出侧/推理侧三处 vehicle_state 与图像预处理完全一致（已实测验证）；30fps 由系统上限锁死、缩放走 CPU vImage 避开 GPU 竞争、CGEvent 周期重发 keyDown、releaseAll 无条件清残留、暖机防启动误降级、OCR 死锁 30s 兜底、真实经过时间累加——这些都是专业级设计。

**为什么不是"极好/极致"**：见下方"必须修的 Bug"与"评级理由"。

> 模块评级分布：CaptureEngine=极好、KeyboardMonitor=极好、SpeedOCRReader=极好；InferenceEngine=良好、ConfidenceEstimator=良好、DegradeStateMachine=良好、ControlEngine=良好、YoloEngine=良好、AuroraDriveApp=良好、RecordEngine=良好、EscapeController=良好、Python 训练链路=良好；RuleController=较好、GameMapView=较好。

---

## 二、必须修的 Bug（按优先级）

### 🔴 P0-1 降级状态机可被 UI 触发永久死锁
`DegradeStateMachine.swift:176`
```swift
let recov = min(1.0, degradeHealth + recoverHysteresis)   // 恢复阈值（滞回）
```
回升条件（`:188`、`:195`）是 `clampedHealth > recov`。当 `degradeHealth + recoverHysteresis ≥ 1.0` 时，`recov` 被 `min` 封到 `1.0`，而 `clampedHealth` 也被夹在 `[0,1]`，于是回升要求 `health > 1.0` 永远为假。
- **触发路径**：UI 降级滑块（`AuroraDriveApp.swift:1948` 区间 `0.3...0.9`）拉到 ≥0.85 时，`recov = min(1.0, 1.0) = 1.0`。状态机一旦掉到 `.yolo`/`.rule` 就**永久卡死在最低档**，即使模型恢复满分置信也回不去。
- **本质**：把"恢复阈值"和"健康度上限"用同一个 `min(1.0,…)` 绑死，制造了不可逾越的边界（被"封顶保护"掩盖的逻辑死锁）。
- **修复（走正规设计，非补丁）**：`recov = min(0.98, degradeHealth + recoverHysteresis)`，并独立约束滑块上限使其与 `recoverHysteresis` 之和恒 `< 1.0`；或改 `clampedHealth >= recov` 并避免 `recov==1.0`。务必加单测覆盖 `degradeThreshold=0.9` 的回升路径。

### 🔴 P0-2 档3「脱困中」从未独立显示（铁律正面违反）
`AuroraDriveApp.swift:190-195` + `:201-213`
```swift
var uiGroup: DriveModeGroup {
    switch self {
    case .e2e, .yolo:     return .e2eDrive
    case .recover, .rule: return .ruleFallback   // ← 脱困中与规则兜底合并
    }
}
```
铁律明确要求"**档3 脱困中(必显示)**"。但 UI 只渲染 `DriveModeGroup`（2 个用户可见档位），操作员在最高风险动作（倒车/转向脱困）期间**永远看不到独立的"脱困中"告警**，误以为仍在"规则兜底"稳态。
- **修复**：新增 `DriveModeGroup.recovering = "脱困中"`，`recover` 单独归属，状态条/芯片赋予独立高亮色与告警样式。

### 🔴 P0-3 YOLO 输出张量格式与通道序假设未经对拍验证（可能静默失效）
`YoloEngine.swift:714-718`（坐标 `÷640` 当像素）、`:672-674`（硬假定 BGR）
- Swift 假设 yolo26s 输出单张量 `[1,maxDet,6]`、`[x1,y1,x2,y2,conf,class]`、**像素坐标**；但 Ultralytics `nms=False` 原始导出通常为 `[1,4+nc,proposals]` 通道优先 + **归一化 xywh**。现存 `verify_yolo_coreml.py` 只覆盖已弃用的 FastestV2(352)，**没有 yolo26s 的对拍脚本**。
- **若坐标约定错**（归一化被当像素再 ÷640）→ 所有框塌缩到画面中心 → `RuleController.isInDangerZone` 的 `y>0.45` 永远不满足 → **避障永久失效且静默无报错**。
- **若通道序错**（R/B 互换）→ 静默误检。
- **修复（一锤定音）**：补 `verify_yolo26s_coreml.py`，对已知帧同时跑 Python 参考与 Swift `rawDump` 逐元素核对：① shape 是否真 `[1,maxDet,6]`；② 坐标是否真像素（否则去掉 `÷640`）；③ class_id 是否内嵌；④ 通道序 BGR 还是 RGB。

### 🔴 P0-4 train_mono 导出链路会产出随机骨干权重
`src/model.py:554-559`（`export_onnx_mono`）、`src/train_mono.py:394-402`（`convert_torch_to_coreml`）
- `build_m9_mono(deploy=True)` 构造 `RepVGGBlock` 只有 `rbr_reparam`（纯 3×3），而 `train_mono` 保存的 `best_model.pt` 是**训练态**（`rbr_3x3/rbr_1x1/rbr_identity`，保存前未 `reparameterize`）。`load_state_dict(sd, strict=False)` 的键与 `rbr_reparam` 无一匹配 → RepVGG-A0 骨干保留**随机初始化**，仅 FusionHead 加载成功 → 推理输出随机。
- 对照正确实现 `tools/export_game_assist_coreml.py:73-83` 用 `has_reparam` 检测 + `deploy=False→load→reparameterize()`。当前部署若走 `export_game_assist_coreml.py`（已修）则 OK，但 `train_mono` 内联导出是雷区。
- **修复**：保存前 `model.reparameterize()`，或导出函数改 `deploy=False` 构造 + 加载 + `reparameterize()`（照 `export_game_assist_coreml.py`）。

### 🟠 P1-1 编译期路径 `#filePath` 写死（部署脆弱）
`AuroraDriveApp.swift:704`、`RecordEngine.swift:109-114`、`InferenceEngine.swift:124-133`
```swift
let modelsDir = URL(fileURLWithPath: #filePath).deletingLastPathComponent().appendingPathComponent("models")
```
`#filePath` 是**编译期源文件路径字面量**（= 开发者机上的源路径）。当前能工作只是因为二进制恰从项目根运行。**换机器/换目录部署**（尤其甲方场景）时模型目录 `models/` 与录制根 `data/raw_clips/` 全部定位失败。
- 修正点：同机重签挪动二进制反而**不受影响**（因为 `#filePath` 是编译期常量，不随二进制位置变）；真实风险是"换机器/换目录"。但无论如何应改运行时路径（`CommandLine.arguments[0]` / `ProcessInfo` 取可执行路径 → `deletingLastPathComponent` / `Bundle`）。

---

## 三、设计风险（"无补丁"原则残留 + 正确性边界）

| # | 位置 | 问题 | 性质 |
|---|---|---|---|
| R1 | `EscapeController.swift:110` | `escapeDirection = Bool.random()` 掷硬币选脱困方向——铁律禁止的绕行式写法。安全上可接受（至少不往同一边死撞），但应改基于障碍方位/车头朝向的确定性决策 | 补丁残留 |
| R2 | `RuleController.swift:53-56` | 危险区 `dangerHalfWidth=0.18`（仅中间 36% 带），左侧来车(x≈0.3)/右侧障碍(x≈0.7)**完全不进避障**；`urgency` 仅看框面积不反映深度 | 漏判偏侧障碍 |
| R3 | `AuroraDriveApp.swift:927` + `EscapeController.swift:65` | 脱困边沿 enter 一次跑 15s 周期，超时后 `isEscaping=false` 返回 idle，但 mode 仍 `.recover`、边沿已过去 → **15s~30s 之间车发呆不重试**（30s 状态机兜底才转 `.rule`） | 发呆死区 |
| R4 | `ControlEngine.swift:70-81` | 事件源用了 `.hidSystemState`，与你的要求".combinedSessionState"**相反**。代码注释称".combinedSessionState 对该游戏无效"。需用目标游戏实测确认哪个真能驱动，结论固化进注释 | 规格待确认 |
| R5 | `InferenceEngine.swift:9` vs `:320` | 文件头注释 vehicle_state 契约写 `[speed,rpm,gear,...]`（旧），实际 `buildVehicleState` 喂 `[speed/120,0,0,1,limit/120,0]`。注释会咬人 | 文档债 |
| R6 | `RuleController.swift:121/134/141/148` + `InferenceEngine.swift:842` | 规则置信度硬编码 0.4/0.5/0.6/0.7、占位 `0.9`、脱困固定 `0.3`——魔法数字，与真实风险无数学关系 | 补丁残留 |
| R7 | `src/mono_dataset.py:117-120` vs `InferenceEngine.swift:320` | v1_old 训练 vehicle_state 全零，推理喂非零 → 模型对 state 无有效梯度，推理落 OOD 区；直接导致 `m9_mono` throttle/brake loss≈0、车大概率不加速 | 训练-推理错位 |
| R8 | `GameMapView.swift:569-574` | `filteredMarkers` 直接 `return []`——"按种类筛选/选择地图"两交互是空壳（数据源被硬关） | 功能缺口 |
| R9 | `AuroraDriveApp.swift` 全仓 | 小地图→大地图实时跟随(≥5Hz) 在审查的 Swift 文件中不存在（按需弹出的全屏 modal，非常驻小地图） | 功能缺口待确认 |
| R10 | `AuroraDriveApp.swift:813/869` | 假速度 `effectiveSpeed -= 6` 待机衰减 + `*= 0.9` OCR 失效衰减，与卡死判定耦合（OCR 永久失效→`speedValid` 恒 false→永不进 `.recover`） | 补丁残留 |
| R11 | `InferenceEngine.swift:147` + `YoloEngine.swift:158` | 三模型全 `computeUnits=.all` 抢同一块 ANE → 热节流 → 越跑越卡（延迟审计报告 A 类根因） | 性能 |

---

## 四、可优化项（非阻塞）

- O1 录制帧被拉伸为 224×224 方形（`RecordEngine.swift:358-377`），与游戏 16:9 不一致；建议与模型输入一致的 180×320 且保持宽高比。
- O2 录制 `perspective` 字段恒为 `"clip"`（`RecordEngine.swift:216-218`，`components(separatedBy:"_").first` 永远取 "clip"）。
- O3 屏幕录制权限被拒后无深链引导（`CaptureEngine.swift:142-148`），重签后用户需手动先关再开，App 不引导。
- O4 `InferenceEngine.swift:268` `readScalar` 假设输出 shape `[1,1]`，若模型误导出 1-D 会静默返回 0；加载时断言输出 shape 即可杜绝。
- O5 三套录制器契约分裂（0基JPG / 5位PNG / 1基JPG），建议统一到 `aurora_recorder.c` 的 0 基 JPG。
- O6 `verify_recording.py` 表头断言过期（5 列 vs C 录制器 7 列）→ 永远假阳性。
- O7 验证集被数据增强污染（`mono_dataset.py:163-202` + `train_mono.py:140-143`）→ `best_val_loss` 落在 epoch 0，选点失真。
- O8 YoloEngine/SpeedOCR 头注释残留 352×352/letterbox 旧描述，易误导维护。

---

## 五、评级理由（对照四档）

| 档位 | 含义 | 本项目达标？ |
|---|---|---|
| 较好 | 基本能跑、有明显草率/未处理项 | ✗ 底层契约实测正确、架构专业，远好于"草率" |
| **良好** | **工程扎实、可运行、有少量系统性缺陷** | **✓ 当前档位** |
| 极好 | 铁律契合、无补丁、全链路对拍验证 | ✗ 有铁律违反(P0-2)+死锁(P0-1)+补丁残留(R1/R6/R10)+感知未对拍(P0-3) |
| 极致 | 零铁律违反、零补丁、训练-推理完全一致、对拍全覆盖 | ✗ 训练-推理 state 错位(R7)、导出随机骨干(P0-4)未根治 |

**修到"极好"的最短路径**：P0-1（死锁，加单测）→ P0-2（铁律违反）→ P0-3（YOLO 对拍）→ P0-4（导出 reparameterize）→ R1/R6/R10（清补丁）→ R7（state 对齐）。修完这些，整体可从"良好"稳定迈向"极好"。

---

## 六、已确认的良好实践（必须保留，勿动）

- 30fps 由系统上限锁死（`CaptureEngine.swift:189` `minimumFrameInterval=1/30`），主线程极轻，缩放走 CPU vImage 避 GPU 竞争。
- CGEvent 周期重发 keyDown（`ControlEngine.swift:162 refreshHeldKeys`）+ releaseAll 无条件清残留（`:172`）——"事件流干涸"已根治。
- 置信度公式已修：极端度只判 `|steer|>0.95`（`ConfidenceEstimator.swift:107`），油门/刹车贴边不判退化；恒定输出不判退化。
- 暖机防启动误降级、OCR 死锁 30s 兜底、卡死 `speed<3·3s`（严格符合铁律）、真实经过时间累加。
- 崩溃防护：YOLO NaN 框跳过、isInferencing 失败复位、模型加载冷却、generation 防过期、OCR 三层校验封顶 `[0,300]` 绝不越界喂崩模型。
